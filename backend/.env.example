# Production Environment Variables (REQUIRED for deployment)
DATABASE_URI=your-production-mongodb-connection-string
PAYLOAD_SECRET=your-production-secret-key-32-characters-minimum
NODE_ENV=production
PORT=3002

# Frontend URL for CORS (update with your actual domain)
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Example for MongoDB Atlas:
# DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net/cms-website

# Example for Railway MongoDB:
# DATABASE_URI=mongodb://mongo:password@mongodb.railway.internal:27017